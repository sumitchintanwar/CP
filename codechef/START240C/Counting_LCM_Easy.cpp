#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve()
{

    ll Z;
    cin >> Z;

}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}
